name: school_sync_engine
description: Durable encrypted offline operation queue contracts for school mobile apps.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  school_mobile_core: ^0.1.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
