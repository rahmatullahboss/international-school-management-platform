name: school_family_domain
description: Immutable guardian and student read models for the School Family application.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  school_mobile_core: ^0.1.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
