name: school_secure_documents
description: Step-up authenticated no-store document exchange for School mobile apps.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: '>=3.44.0'

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  http: ^1.6.0
  path_provider: ^2.1.6
  school_authentication: ^0.1.0
  school_family_domain: ^0.1.0
  school_mobile_core: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
