name: school_staff_domain
description: Immutable teacher journey contracts for the School Staff application.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  school_mobile_core: ^0.1.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
