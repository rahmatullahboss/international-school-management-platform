name: school_app_bootstrap
description: Shared signed-in lifecycle, access selection and bootstrap UI for school mobile apps.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  school_api_client: ^0.1.0
  school_authentication: ^0.1.0
  school_design_system: ^0.1.0
  school_mobile_core: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
