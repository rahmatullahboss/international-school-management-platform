name: school_mobile_core
description: Shared tenant, persona, capability and synchronization contracts.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
