name: Family Interaction Localization Apply

on:
  pull_request:
    paths:
      - 'mobile/apps/family_app/lib/main.dart'
      - 'mobile/apps/family_app/lib/family_interaction_screens.dart'
      - 'mobile/apps/family_app/lib/family_interaction_strings.dart'
      - 'mobile/apps/family_app/test/family_interaction_strings_test.dart'
      - 'mobile/tool/apply_family_interaction_localization.py'
      - 'mobile/tool/run_family_interaction_localization_apply.py'
      - '.github/workflows/family-interaction-localization-apply.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.44.7'
          cache: true
      - name: Apply bounded Family interaction localization wiring
        run: python3 mobile/tool/run_family_interaction_localization_apply.py
      - name: Format changed Dart files
        run: |
          dart format \
            mobile/apps/family_app/lib/main.dart \
            mobile/apps/family_app/lib/family_interaction_screens.dart \
            mobile/apps/family_app/lib/family_interaction_strings.dart \
            mobile/apps/family_app/test/family_interaction_strings_test.dart
      - name: Commit applied localization wiring
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'Family interaction localization wiring already applied.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            mobile/apps/family_app/lib/main.dart \
            mobile/apps/family_app/lib/family_interaction_screens.dart \
            mobile/apps/family_app/lib/family_interaction_strings.dart \
            mobile/apps/family_app/test/family_interaction_strings_test.dart
          git commit -m 'feat(mobile): localize Family interaction journeys'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
