name: CI

on:
  pull_request:

permissions:
  contents: write

jobs:
  author-pilot-04-governance:
    if: github.head_ref == 'docs/pilot-04-database-read-model-evidence' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/pilot-04-database-read-model-evidence
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22.22.2'
          cache: npm
      - run: npm ci
      - name: Synchronize PILOT-04 tracker
        run: |
          python3 scripts/sync_pilot_04_tracker.py
          rm scripts/sync_pilot_04_tracker.py
      - run: npm run format
      - run: python3 scripts/validate_execution_artifacts.py
      - name: Commit verified PILOT-04 governance
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'docs(pilot): synchronize database read-model tracker'
          git push origin HEAD:docs/pilot-04-database-read-model-evidence

  verify:
    needs: author-pilot-04-governance
    if: always() && needs.author-pilot-04-governance.result == 'skipped'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Canonical verification resumes after governance synchronization.'
