name: INT-01 Neon Gate

on:
  push:
    branches:
      - module/international-integrations
  workflow_dispatch:
    inputs:
      operation:
        description: Neon gate operation
        required: true
        default: inspect
        type: choice
        options:
          - inspect
          - apply
          - replay-database

permissions:
  contents: read

concurrency:
  group: int-01-neon-gate
  cancel-in-progress: false

jobs:
  neon-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      DATABASE_URL: ${{ secrets.INT01_DATABASE_URL }}
      COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
    steps:
      - uses: actions/checkout@v4

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install --yes postgresql-client

      - name: Resolve operation
        id: operation
        env:
          DISPATCH_OPERATION: ${{ inputs.operation }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          mode=inspect
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            mode="${DISPATCH_OPERATION:-inspect}"
          elif [[ "$COMMIT_MESSAGE" == *"[neon-replay-db]"* ]]; then
            mode=replay-database
          elif [[ "$COMMIT_MESSAGE" == *"[neon-apply]"* ]]; then
            mode=apply
          fi
          echo "mode=$mode" >> "$GITHUB_OUTPUT"
          echo "Resolved INT-01 Neon gate mode: $mode"

      - name: Resolve database availability
        id: database
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [[ -n "$DATABASE_URL" ]]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "available=false" >> "$GITHUB_OUTPUT"
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            echo "INT01_DATABASE_URL repository secret is required" >&2
            exit 1
          fi
          echo "INT01_DATABASE_URL is not configured; live Neon gate remains pending."

      - name: Run INT-01 Neon gate
        if: steps.database.outputs.available == 'true'
        run: bash scripts/int-01-neon-gate.sh "${{ steps.operation.outputs.mode }}"
